<Window x:Class="EnvironmentVarsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EnvironmentVarsApp"
        xmlns:vm="clr-namespace:EnvironmentVarsApp.ViewModels"
        mc:Ignorable="d"
        Title="Переменные среды" Height="450" Width="800">
    
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Управление переменными среды" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <!-- DataGrid с переменными -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding EnvironmentVariables}"
                  SelectedItem="{Binding SelectedVariable}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя переменной" 
                                    Binding="{Binding Name}" 
                                    IsReadOnly="True"
                                    Width="200"/>
                <DataGridTextColumn Header="Значение" 
                                    Binding="{Binding Value, UpdateSourceTrigger=Explicit}" 
                                    Width="300"/>
                <DataGridTextColumn Header="Комментарий" 
                                    Binding="{Binding Comment, UpdateSourceTrigger=Explicit}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Сохранить все" 
                    Width="100" 
                    Margin="0,0,10,0"
                    Command="{Binding SaveAllVariablesCommand}"/>
            <Button Content="Очистить" 
                    Width="100" 
                    Margin="0,0,10,0"
                    Command="{Binding DeleteVariableCommand}"/>
            <Button Content="Обновить" 
                    Width="100" 
                    Command="{Binding RefreshVariablesCommand}"/>
        </StackPanel>
    </Grid>
</Window>
